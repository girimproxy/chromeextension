{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.0.0-alpha.6",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "0.9.5",
    "react-transition-group": "^1.2.0",
    "reactstrap": "^4.8.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -R icons build/ && cp manifest.json build/ && cp background.js build/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
